# TODO - Fix test results fetching/saving

- [x] Update `backend/routes/results.js` to set `user` from `req.body.user` when `req.user` is missing.

- [ ] (Optional) Add minimal console logging for saved user/testType.
- [x] Verify backend now saves `user` using body user when `req.user` missing.
- [ ] Run backend and confirm saved tests appear under the user’s profile.


